(()=>{
const C=window.NJ_CONFIG,D=window.NJ_DRAW,$=s=>document.querySelector(s),els={title:$('#title'),weekLabel:$('#weekLabel'),channel:$('#channel'),theme:$('#theme'),fontStyle:$('#fontStyle'),decoration:$('#decoration'),avatarFile:$('#avatarFile'),avatarPreview:$('#avatarPreview'),days:$('#days'),canvas:$('#scheduleCanvas'),status:$('#status')};
const COVER_OPTIONS=Array.isArray(window.NJ_COVERS)?window.NJ_COVERS:[];
let avatarData='',saveTimer,renderToken=0;
function escapeHtml(v){return String(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function coverOptions(selected=''){const base=['<option value="">Auto / Nessuna</option>'];for(const file of COVER_OPTIONS){const safe=escapeHtml(file);base.push(`<option value="${safe}" ${file===selected?'selected':''}>${safe}</option>`)}return base.join('')}
function rows(){C.days.forEach(day=>{const preset=C.defaults[day.key]?.cover||'';const el=document.createElement('div');el.className='day-row';el.innerHTML=`<input class="day-toggle" type="checkbox" id="${day.key}-enabled" aria-label="Attiva ${day.label}"><input type="text" id="${day.key}-activity" maxlength="38" placeholder="Gioco o attività"><input type="time" id="${day.key}-time"><select class="cover-select" id="${day.key}-cover">${coverOptions(preset)}</select>`;els.days.appendChild(el)})}
function currentWeek(){const now=new Date,day=now.getDay()||7,mon=new Date(now);mon.setHours(12,0,0,0);mon.setDate(now.getDate()-day+1);const sun=new Date(mon);sun.setDate(mon.getDate()+6);const m=d=>d.toLocaleDateString('en-US',{month:'short'}).toUpperCase();els.weekLabel.value=mon.getMonth()===sun.getMonth()?`${String(mon.getDate()).padStart(2,'0')} / ${String(sun.getDate()).padStart(2,'0')} ${m(sun)}`:`${String(mon.getDate()).padStart(2,'0')} ${m(mon)} / ${String(sun.getDate()).padStart(2,'0')} ${m(sun)}`}
function defaults(){els.title.value='STREAM SCHEDULE';currentWeek();els.channel.value='@narcisnj';els.theme.value='lilac';els.fontStyle.value='dynapuff';els.decoration.value='dreamy';avatarData='';preview();C.days.forEach(d=>{const v=C.defaults[d.key];$(`#${d.key}-enabled`).checked=v.enabled;$(`#${d.key}-activity`).value=v.activity;$(`#${d.key}-time`).value=v.time;$(`#${d.key}-cover`).value=v.cover||''})}
function state(){const schedule={};C.days.forEach(d=>schedule[d.key]={enabled:$(`#${d.key}-enabled`).checked,activity:$(`#${d.key}-activity`).value.trim(),time:$(`#${d.key}-time`).value,cover:$(`#${d.key}-cover`).value.trim()});return{title:els.title.value.trim(),weekLabel:els.weekLabel.value.trim(),channel:els.channel.value.trim(),theme:els.theme.value,fontStyle:els.fontStyle.value,decoration:els.decoration.value,schedule}}
function apply(s){els.title.value=s.title??'STREAM SCHEDULE';els.weekLabel.value=s.weekLabel??'';els.channel.value=s.channel??'@narcisnj';els.theme.value=s.theme??'lilac';els.fontStyle.value=s.fontStyle??'dynapuff';els.decoration.value=s.decoration??'dreamy';avatarData=s.avatarData??'';preview();C.days.forEach(d=>{const v=s.schedule?.[d.key]??C.defaults[d.key];$(`#${d.key}-enabled`).checked=!!v.enabled;$(`#${d.key}-activity`).value=v.activity??'';$(`#${d.key}-time`).value=v.time??'';$(`#${d.key}-cover`).value=v.cover??''})}
function persist(show=false){try{const s=state();s.avatarData=avatarData;localStorage.setItem(C.storageKey,JSON.stringify(s));if(show)flash('✓ salvato nel browser')}catch{flash('salvataggio locale non disponibile')}}
function restore(){try{const raw=localStorage.getItem(C.storageKey);raw?apply(JSON.parse(raw)):defaults()}catch{defaults()}}
function preview(){if(avatarData){els.avatarPreview.src=avatarData;return}const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="96" height="96"><defs><linearGradient id="g" x1="0" x2="1" y1="0" y2="1"><stop stop-color="#d6b8f4"/><stop offset="1" stop-color="#7c4ca7"/></linearGradient></defs><rect width="96" height="96" rx="28" fill="url(#g)"/><text x="48" y="59" text-anchor="middle" font-size="28" font-family="Arial" font-weight="900" fill="white">NJ</text></svg>`;els.avatarPreview.src='data:image/svg+xml;charset=utf-8,'+encodeURIComponent(svg)}
function flash(msg){const old=els.status.textContent;els.status.textContent=msg;clearTimeout(els.status._t);els.status._t=setTimeout(()=>els.status.textContent=old,1700)}
function scheduleSave(){clearTimeout(saveTimer);saveTimer=setTimeout(()=>persist(false),250)}
async function render(){const token=++renderToken;try{await document.fonts.ready}catch{}if(token!==renderToken)return;await D.renderSchedule({canvas:els.canvas,state:state(),avatarData,tokenValid:()=>token===renderToken})}
async function download(){await render();const a=document.createElement('a'),safe=(els.weekLabel.value||'week').replace(/[^a-z0-9_-]+/gi,'-').replace(/^-|-$/g,'').toLowerCase();a.download=`nj-stream-schedule-${safe}.png`;a.href=els.canvas.toDataURL('image/png');a.click();flash('✓ png creato')}
rows();restore();render();document.querySelectorAll('input,select').forEach(el=>{el.addEventListener('input',()=>{render();scheduleSave()});el.addEventListener('change',()=>{render();scheduleSave()})});els.avatarFile.addEventListener('change',e=>{const f=e.target.files?.[0];if(!f)return;const r=new FileReader;r.onload=()=>{avatarData=String(r.result||'');preview();render();scheduleSave()};r.readAsDataURL(f)});$('#currentWeekBtn').addEventListener('click',()=>{currentWeek();render();scheduleSave()});$('#saveBtn').addEventListener('click',()=>persist(true));$('#downloadBtn').addEventListener('click',download);$('#resetBtn').addEventListener('click',()=>{if(!confirm('Ripristinare il programma di esempio?'))return;localStorage.removeItem(C.storageKey);defaults();persist();render();flash('reset completato')});
})();
